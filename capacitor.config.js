/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.apex.ridetracker',
  appName: 'Apex',
  webDir: 'dist',
  ios: {
    contentInset: 'always',
  },
};

export default config;
